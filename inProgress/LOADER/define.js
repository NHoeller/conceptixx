/**
 * @project : modula.js
 * @package : loader
 * @internal : loader.intro
 * @type : constructor
 * @dependencies : none
 *
 * @description :
 * the main part of the modula is a self extending loader object
 */


		/**
		 * pre init
		 */
		// get the primary script tag of the modula
		var Script = document.getElementById( 'modulajs' );
		// define HashTag
		var HashTag
		// check if we have primary script and get HashTag
		if( Script && ( HashTag = Script.dataset || Script.title || Script.class ) {
// todo
		}


		/**
		 * init modula by an anonymous function
		 */
		( function( context ) {
		}


